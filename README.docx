项目框架与介绍
项目概述
本项目是一个基于STM32F103C8微控制器和FreeRTOS实时操作系统开发的嵌入式多任务通信系统。该系统旨在演示FreeRTOS环境下任务间的通信机制，包括消息队列、互斥锁和同步标志位的使用。项目通过多个任务协同工作，实现传感器数据采集、数据传输、显示控制和LED状态管理等功能。系统集成了多种外设，如ADC模数转换器、串口通信、OLED显示屏和LED指示灯，并通过ESP32模块实现无线数据传输。项目采用模块化设计，便于维护和扩展。

软件框架
操作系统：FreeRTOS v10.x（嵌入式实时操作系统，提供任务调度、队列、信号量等机制）。
编程语言：C语言。
开发环境：Keil MDK（基于STM32标准外设库）。
任务结构：
Task_A（优先级2）：系统初始化任务，负责串口、ADC和ESP32模块的初始化。初始化完成后自删除，确保其他任务在系统稳定后启动。
Task_B（优先级1）：数据采集任务，定时读取ADC值并发送到消息队列，供其他任务使用。同时通过互斥锁保护OLED显示资源。
ask_C（优先级1）：数据接收与显示任务，从消息队列接收ADC数据，并在OLED上显示。使用互斥锁避免显示冲突。
Task_D（优先级1）：指令控制任务，根据串口接收的指令（如"LED1_ON"）控制LED状态，并在OLED上显示指令内容。
通信机制：
消息队列（xQueue_BC）：用于Task_B向Task_C传递ADC数据，队列长度5，元素大小uint32_t。
互斥锁（xOLEDMutex）：保护OLED显示资源，避免多任务同时访问导致冲突。
同步标志位（flag）：Task_A完成后设置标志位，其他任务检查标志位后启动。
中断处理：
USART1中断：处理串口数据接收，使用状态机解析数据包。
TIM2中断：定时触发ADC数据发送到ESP32模块。
堆栈监控：每个任务监控堆栈高水位标记，若低于50字节则在OLED上显示错误信息。
硬件框架
主控芯片：STM32F103C8（Cortex-M3内核，72MHz主频，64KB Flash，20KB SRAM）。
外设模块：
ADC：用于模拟信号采集（单通道，连续转换模式，DMA传输）。
串口：USART1，用于与ESP32模块通信（波特率115200）。
OLED显示屏：0.96英寸I2C接口OLED屏，用于显示系统状态、ADC数据和指令。
LED指示灯：两个LED，用于状态指示和指令控制。
定时器：TIM2，用于定时中断（周期约2.78秒，预分频7200，计数20000）。
ESP32模块：通过串口连接，实现无线数据传输（WiFi/Bluetooth）。
电源与时钟：外部晶振8MHz，内部PLL倍频到72MHz。
引脚分配
基于代码分析，以下是主要外设的引脚配置（GPIO模式和功能）：

外设模块	引脚	功能描述	GPIO配置
LED1	PA1	LED1控制（低电平点亮）	输出推挽，50MHz
LED2	PA3	LED2控制（低电平点亮）	输出推挽，50MHz
串口 (USART1)	PA9	TX发送	复用推挽输出，50MHz
串口 (USART1)	PA10	RX接收	上拉输入，50MHz
ADC (ADC1)	PA0	模拟输入通道0	模拟输入模式
OLED (I2C)	PB8	SCL时钟线	开漏输出，50MHz
OLED (I2C)	PB9	SDA数据线	开漏输出，50MHz
定时器 (TIM2)	-	无外部引脚（内部中断）	-
注意：所有GPIO时钟均通过RCC_APB2PeriphClockCmd开启。ADC使用DMA1通道1进行数据传输。ESP32模块复用USART1串口，无额外引脚分配。
系统工作流程
启动：main函数初始化OLED显示启动信息，延时后调用System_Start创建任务。
初始化：Task_A初始化硬件，设置同步标志位后自删除。
数据采集：Task_B循环读取ADC值，发送到队列，并检查堆栈。
数据处理：Task_C从队列接收数据，显示在OLED上。
控制响应：Task_D监听串口指令，控制LED并显示指令。
定时传输：TIM2中断定期将ADC数据发送到ESP32模块。
依赖与配置
FreeRTOS配置：使用标准配置，支持任务调度、队列和信号量。堆大小通过configTOTAL_HEAP_SIZE定义。
库文件：STM32F10x标准外设库（Library文件夹），包括GPIO、ADC、USART、TIM等。
编译设置：Keil项目文件（.uvprojx），目标芯片STM32F103C8，调试配置使用J-Link或ST-Link。
内存使用：任务堆栈大小（Task_A:128字节，Task_B:256字节，Task_C:128字节，Task_D:128字节）。
该项目展示了FreeRTOS在嵌入式系统中的应用，强调任务间通信的可靠性与效率，适用于物联网、工业控制等场景。代码结构清晰，易于理解和修改。
